/**
 * Behavior Bundles - Pre-configured behavior compositions
 * 
 * This module provides reusable behavior bundles that encapsulate common patterns
 * used across different workout strategies. Using bundles ensures consistent
 * behavior composition, explicit dependencies, and reduces code duplication.
 */

export { TimerBundle, type TimerBundleConfig } from './TimerBundle';
export { LoopBundle, type LoopBundleConfig } from './LoopBundle';
