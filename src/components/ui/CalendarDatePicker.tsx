/**
 * CalendarDatePicker — re-export shim for backward compatibility.
 * The canonical component is now CalendarCard.
 */

export { CalendarCard as CalendarDatePicker } from './CalendarCard';
export type { CalendarCardProps as CalendarDatePickerProps } from './CalendarCard';
