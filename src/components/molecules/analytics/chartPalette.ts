/** Series palette for analytics charts — maps onto the metric token set. */
export const SERIES_COLORS = [
  'hsl(var(--metric-time))',
  'hsl(var(--metric-effort))',
  'hsl(var(--metric-rep))',
  'hsl(var(--metric-rounds))',
  'hsl(var(--metric-distance))',
  'hsl(var(--metric-resistance))',
  'hsl(var(--metric-action))',
  'hsl(var(--primary))',
];
