# Improve — Architecture friction findings

> Generated by an architecture review walk on 2026-06-19.
> Follows the `/improve-codebase-architecture` skill process: explore,
> present candidates, grill. These are the **candidates**; the grilling loop
> is the next conversation.

The skill is informed by `CONTEXT.md` and `docs/06-interfaces-and-implementations.md`.
The vocabulary is the skill's (`module`, `interface`, `seam`, `adapter`,
`depth`, `locality`, `leverage`), not the codebase's general "service /
component / API" language. Each document below uses that vocabulary.

## Documents

| # | Document | What it covers |
|---|----------|----------------|
| 00 | [Meta-findings](./00-meta-findings.md) | Stale cast plan, missing `docs/adr/`, four `CONTEXT.md` concept gaps, ranked order of the six findings |
| 01 | [Parser & compiler are both Dialect homes](./01-parser-compiler-dialect-stages.md) | The "parser is unit-free" promise is broken; introduce `DialectStage` |
| 02 | [`src/timeline/analytics/` is a misnomered home](./02-timeline-analytics-misnomer.md) | Move projection engines into `core/analytics/engines/`, delete the shim |
| 03 | [`workbenchSyncStore` is 5 stores in a trench coat](./03-workbench-sync-store-split.md) | Split the god store into `runtimeSyncStore` / `documentStore` / `analyticsStore` / `viewModeStore` |
| 04 | [`ReceiverApp` has 3 init paths](./04-receiver-app-three-init-paths.md) | Collapse to one `attachSessionToReact` helper in `ReceiverSessionManager` |
| 05 | [`ScriptRuntime` is 5 jobs in a class](./05-script-runtime-five-jobs.md) | Reframe around `ActionTurn` + `RuntimeObserverBus`; closes the `mixed-timers.md` bug at the architecture level |
| 06 | [`MarkdownCanvasPage` is 5 sub-jobs in a page](./06-canvas-page-five-subjobs.md) | Pull panel-mode logic into `useCanvasRuntime`; render the rest |

Each finding includes a **Path at a glance** diagram (one component-level
flow showing the existing situation) and a **## Diagrams** section near the
end with 2-3 small component-level mermaid diagrams (existing → proposed,
plus a symptom or precedent where it adds clarity). All diagrams stay at a
single C4 level (Component) and focus on the path between modules — not on
data shape or layer topology.

## Process followed

- **Explore:** walked the codebase organically via Repowise MCP tools
  (`get_answer`, `get_context`, `get_symbol`, `get_why`, `get_risk`); read
  the test harness, the testing gap analysis, the cast plan, the
  `CONTEXT.md` glossary.
- **Verify:** re-fetched the key line citations before writing (see each
  document's "Files involved" table for line counts and snippets).
- **Present:** the original chat reply surfaced all six candidates as a
  numbered list with confidence, seam test, and risk per item; this folder
  is the written record.

## Next step

Pick a candidate and drop into the **grilling loop**: walk the design tree
(constraints, dependencies, the shape of the deepened module, what sits
behind the seam, what tests survive). Decisions crystallize inline:

- New domain terms get added to `CONTEXT.md` (`Dialect Stage`,
  `Mounted Block`, `Block Lifecycle`, `Workbench Effects` — see Meta).
- Rejected candidates with a load-bearing reason get offered as ADRs.
- Sharper terminology updates `CONTEXT.md` immediately.

## Skip list (from the walk)

- **Cast refactor (Phase 1-4)** — already executed; the plan is stale.
- **`mergeFragments` → `fuseUnits` alone** — covered by Finding 01.
- **Store ↔ context migration for analytics only** — covered by Finding 03.
- **`ProjectionResult` deletion standalone** — covered by Finding 02.
- **`ExecutionContext` extraction standalone** — covered by Finding 05.
- **`AnalyticsEngine.addStage` shim deletion** — mechanical, not architecture.
- **`ConcreteBehaviorFactory` expansion** — feature, not architecture.
- **Tests for known timer bugs** — not architecture; Finding 05 is the
  precondition.
