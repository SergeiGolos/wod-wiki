
the sticky section should be able to load with a subsection to avoid wasting scroll space... and lest script through the first act with the Pushup Branchmarks.

---

## Current
Write your workout
WhiteboardScript uses plain text to define workout structure. Every line you type becomes a block the timer can execute.

## Update

{now make sticky}
Step 1 · Plan
Your Workouts as Markdown
WodWiki uses plain text **markdown**, but sprinkles some parsing magic on the special `'''wod`  sections to understand workout structure and and create smart **lap timers**  to extract and collkect the woprkout **metrcis**.  

{prelaod subsection}

Scroll to explore 

## Current

Step 1 · Plan
Colors map to metrics

Syntax highlighting shows you what the timer sees. Green = movement, blue = duration, orange = reps, purple = effort or weight.

## Update

{now a sub heading}
Colors map to metrics

The editor hints at **metrics**  being collection with some "syntax" highlighting. 

- Create a list form this, and with each item reference as a list item, with the example text for each visible in the ui.  include a short detail of how the parse identifies the sections.
  
  
## Curent

Step 1 · Plan

## Ready to run

Click the ▶ Run button in the wod block header — or the inline button — to hand off to the timer.

( ▶ Run )

## Update

Step 1 · Plan

## Ready to run

Click the ( Run ) button in the wod block header — or the inline button — to hand off to the timer.

---

collection is next, but this is worong,    we now move on to the nbext sticky header but this keeping the asame sticky view with the timer now ruinning

## Current

Step 2 · Track

Timer counts up

The `5 Pushups` block has no time set, so the timer counts up. Your actual rep time becomes the recorded metric.


## Updated 

Step 2 · Track
{now make sticky}

The session is started, and the primary button that controls is your lap / next button that lets you step though the specialized workout timer build based on the **metrics** provider in the edit.

{{if at this point the timer is not yet running, start it when scrolling from the top.}}

{now a sub heading}

Timer counts up

The `5 Pushups` block has no time set, so the timer counts up. Your actual rep time becomes the recorded metric.


the nexst elements of stepp follow the same process of using child deadings untill we switch to data view. also to be scrolled though on the existing editor.  then have the existing collection that comes is enabled on the new view that slided in with the select a workout panel loaded.